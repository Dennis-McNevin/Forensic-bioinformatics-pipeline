A systemn administrator needs to run the install_mpsforensics.sh script on the system before a user can use this system.

Once it has been run, put the MPSforensicsSetup.sh script on each user's Desktop. They can double click it to install the application for their use.
